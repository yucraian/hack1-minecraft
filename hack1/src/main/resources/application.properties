spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5555}/${DB_NAME:postgres}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:123}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

jwt.secret=${JWT_SECRET:2bb80d537b1da3e38bd30361aa855686bde0eacd7162fef6a25fe97bf527a25b}
jwt.expiration=${JWT_EXPIRATION_ACCESS:3600000}

spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:franco.gonzalez@utec.edu.pe}
spring.mail.password=${MAIL_PASSWORD:hiiblwigdhujdhdq}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ===== GITHUB MODELS CONFIGURATION =====
github.models.url=${GITHUB_MODELS_URL:https://models.github.com/v1}
github.token=${GITHUB_TOKEN:}
model.id=${MODEL_ID:openai/gpt-5-mini}

# ===== ASYNC CONFIGURATION =====
spring.task.execution.pool.core-size=5
spring.task.execution.pool.max-size=10
spring.task.execution.pool.queue-capacity=100

# ===== SERVER CONFIGURATION =====
server.port=8080
spring.application.name=oreo-insight-factory

# ===== LOGGING =====
logging.level.org.example.hack1=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.mail=DEBUG